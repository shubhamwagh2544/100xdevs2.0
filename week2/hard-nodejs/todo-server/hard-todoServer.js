//Hard todo: Try to save responses in files, 
//so that even if u exit the app and run it again, the data remains(similar to databases)

const express = require('express');
const bodyParser = require('body-parser');
const fs = require('fs');
const path = require('path');
